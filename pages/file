/* General layout */
body {
  display: flex;
  margin: 0;
  font-family: Arial, sans-serif;
}

/* Sidebar style */
.sidebar {
  position: fixed;
  top: 0;
  left: 0;
  width: 250px;
  height: 100vh;
  background: #1f2937;
  color: #fff;
  overflow-y: auto;
  z-index: 1000;
  display: flex;
  flex-direction: column;
  align-items: center;
  transition: width 0.3s ease;
}

.sidebar.collapsed {
  width: 80px;
}

.sidebar-header {
  position: relative; /* makes absolute positioning relative to sidebar header */
  width: 100%;
  display: flex;
  align-items: center;
  flex-direction: column;
  /* justify-content: space-between;
 
  margin-top: 1rem; */
   padding: 20px;
}

#sidebarToggle {
  position: absolute;
  top: 5px;
  right: 5px;
  background: none;
  border: none;
  color: white;
  /* font-size: 1.5rem; */
  cursor: pointer;
  /* margin-bottom: 2rem; */
}


/* Sidebar logo/profile image */
.sidebar .profile-img {
  width: 70px;
  height: 70px;
  border-radius: 50%;
  object-fit: cover;
  border: 2px solid white;
  /* display: block; */
  margin-top: 40px;
  /* transition: width 0.3s ease, height 0.3s ease margin-top 0.3s ease; */

}

/* Smaller logo when collapsed */
.sidebar.collapsed .profile-img {
  width: 40px;
  height: 40px;
  margin-top: 20px;
}

/* Sidebar links */
.sidebar a {
  display: flex;
  align-items: center;
  padding: 10px 15px;
  color: white;
  text-decoration: none;
  margin: 5px 0;
  border-radius: 5px;
  font-size: 14px;
  transition: padding 0.3s ease;
}

.sidebar a:hover {
  background: #34495e;
}

/* Icons inside links */
.sidebar .icon {
  font-size: 1.4rem;
  width: 35px;
  text-align: center;
  margin-right: 10px;
  transition: margin-right 0.3s ease;
}
.sidebar .text{
  font-size: 1rem;
}

/* Hide link text when collapsed */
.sidebar.collapsed a span.text {
  display: none;
}

/* Center icons when collapsed */
.sidebar.collapsed a {
  justify-content: center;
  padding: 10px;
}

/* Content style */
.content {
  flex-grow: 1;
  padding: 20px;
  background-color: #ebeef1ff;
  margin-left: 250px; /* sidebar width */
  transition: margin-left 0.3s ease;
}

/* Adjust content when sidebar collapsed */
.sidebar.collapsed + .content {
  margin-left: 80px;
}

/* Cards styling */
.card {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  border-radius: 12px;
  padding: 15px;
}

/* Form label spacing */
.form-label span {
  margin-left: 3px;
  width: 300px;
}

/* Container width for professionalism */
.content .container {
  max-width: 1400px;
}

/* Responsive cards layout */
.row.g-3 > [class*="col-"] {
  display: flex;
  flex-direction: column;
}

/* Sidebar responsiveness */
@media (max-width: 992px) {
  .sidebar {
    position: relative;
    width: 100%;
    height: auto;
  }
  .content {
    margin-left: 0;
  }
}

/* Small screen fix for cards */
@media (max-width: 576px) {
  .row.g-3 {
    gap: 1rem;
  }
}
