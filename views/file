doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title MWF | Login
    link(rel="stylesheet" href="../css/login.css")
    link(rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css")

  body
    .login-container
      .login-left
        .welcome-text
          h1 Hello!
          p Have a GOOD DAY
      .login-right
        .login-form
          h2 Login
          form#loginForm(action="/login" method="POST")
            .input-group
              input(type="email" name="email" id="email" placeholder="Email" required)
            .input-group
              input(type="password" name="password" id="password" placeholder="Password" required)
            button.btn(type="submit") Login
          p.forgot-password
            a(href="forgot-password.html") Forgot your password?
          .divider
            span OR
          .social-login
            button.social-btn.google(type="button")
              i.fab.fa-google
              |  Continue with Google
            button.social-btn.github(type="button")
              i.fab.fa-github
              |  Continue with GitHub
            button.social-btn.linkedin(type="button")
              i.fab.fa-linkedin
              |  Continue with LinkedIn
          p.signup-link
            | Don’t have an account? 
            a(href="/register-user") Register
    //- script(src="../js/login.js")



    doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title MWF | Login
    link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css")
    style.
      body {
        background: linear-gradient(135deg, #2c3e50, #34495e);
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      }
      .container-login {
        display: flex;
        flex-wrap: wrap;
        width: 90%;
        max-width: 900px;
        background: #fff;
        border-radius: 15px;
        box-shadow: 0 4px 15px rgba(0,0,0,0.2);
        overflow: hidden;
      }
      .login-left, .login-right {
        flex: 1;
        min-width: 300px;
        padding: 2rem;
      }
      .login-left {
        background: #27ae60;
        color: #fff;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        text-align: center;
      }
      .login-left h2 {
        font-weight: 700;
        margin-bottom: 1rem;
      }
      .login-left p {
        font-size: 1rem;
        line-height: 1.5;
      }
      .login-right {
        background: #fff;
      }
      .login-card img {
        display: block;
        margin: 0 auto 1rem auto;
        width: 80px;
      }
      .login-card h3 {
        text-align: center;
        margin-bottom: 1.5rem;
        font-weight: 600;
        color: #2c3e50;
      }
      .form-control:focus {
        border-color: #27ae60;
        box-shadow: 0 0 0 0.2rem rgba(39,174,96,0.25);
      }
      .btn-login {
        background: #27ae60;
        border: none;
        font-weight: 500;
      }
      .btn-login:hover {
        background: #219150;
      }
      .text-muted a {
        text-decoration: none;
        color: #27ae60;
      }
      .text-muted a:hover {
        text-decoration: underline;
      }

  body
    .container-login
      // Left side message for staff
      .login-left
        h2 Welcome to MWF System
        p
          | Staff, please log in to manage stocks, sales, and reports. 
          br
          | Let’s keep MWF running smoothly together!
      
      // Right side login form
      .login-right.login-card
        img(src="/images/mwflogo.png", alt="MWF Logo")
        h3 MWF Login
        form(action="/login" method="POST")
          .mb-3
            label.form-label(for="email") Email
            input#email.form-control(type="email" name="email" placeholder="Enter your email" required)
          .mb-3
            label.form-label(for="password") Password
            input#password.form-control(type="password" name="password" placeholder="Enter your password" required)
          button.btn.btn-login.w-100(type="submit") Login
        p.text-center.text-muted.mt-3
          | Don’t have an account? 
          a(href="/register") Register
